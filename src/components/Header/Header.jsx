import s from "./Header.module.css"

const Header = () => {
    return (
        <header className={s.header}>
            <img src='..\logo-tg.png' />
        </header>
    )
}

export default Header;